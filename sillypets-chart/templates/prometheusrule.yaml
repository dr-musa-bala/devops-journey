apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: sillypets-alerts
  namespace: monitoring
  labels:
    release: prometheus-stack
spec:
  groups:
  - name: sillypets.alerts
    rules:
    # 1. Memory Usage Alert
    - alert: SillypetsHighMemoryUsage
      expr: sum(container_memory_working_set_bytes{namespace="default", pod=~"sillypets.*"}) by (pod) > 50000000
      for: 2m
      labels:
        severity: warning
        namespace: monitoring
      annotations:
        summary: 'High memory usage on pod {{ "{{" }} $labels.pod {{ "}}" }}'
        description: 'Pod {{ "{{" }} $labels.pod {{ "}}" }} is consuming over 50 MB of memory (current value: {{ "{{" }} $value {{ "}}" }} bytes).'

    # 2. CPU Usage Alert (Test Threshold)
    - alert: SillypetsHighCPUUsage
      expr: sum(rate(container_cpu_usage_seconds_total{pod=~"sillypets.*"}[1m])) by (pod) > 0.001
      for: 0s
      labels:
        severity: warning
        namespace: monitoring
      annotations:
        summary: 'High CPU usage on pod {{ "{{" }} $labels.pod {{ "}}" }}'
        description: 'Pod {{ "{{" }} $labels.pod {{ "}}" }} CPU usage is high (current rate: {{ "{{" }} $value {{ "}}" }}).'